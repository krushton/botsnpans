<h1>Bots 'n Pans Levels</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Level category</th>
    <th>Time limit</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @levels.each do |level| %>
  <tr>
    <td><%= level.name %></td>
    <td><%= level.final_state_id %></td>
    <td><%= level.description %></td>
    <td><%= level.level_category.name %></td>
    <td><%= level.time_limit %></td>
    <td><%= link_to 'Show', level %></td>
    <td><%= link_to 'Edit', edit_level_path(level) %></td>
    <td><%= link_to 'Destroy', level, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Level', new_level_path %>
