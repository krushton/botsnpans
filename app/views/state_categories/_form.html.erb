<%= form_for(@state_category) do |f| %>
  <% if @state_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_category.errors.count, "error") %> prohibited this state_category from being saved:</h2>

      <ul>
      <% @state_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
